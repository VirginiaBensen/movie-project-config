replicaCount: 2

image:
  repository: YOUR_ECR_REPOSITORY_URI
  pullPolicy: IfNotPresent
  tag: ""

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: "nginx"
  annotations: {}
  hosts:
    - host: movies.your-awesome-domain.com # <-- ייש לעדכן עתידית
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - movies.your-awesome-domain.com

resources:
  limits:
    cpu: 200m     
    memory: 256Mi 
  requests:
    cpu: 100m     
    memory: 128Mi 

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 10
  targetCPUUtilizationPercentage: 80

postgresql:
  global:
    storageClass: "gp2" 
  auth:
    database: "movie_db"
    username: "Vir"
    password: "1509" 

serviceAccount:
  create: false
  name: ""